[pytest]
# Tests import the backend app as `app.*`, so the backend dir must be importable.
# `pythonpath = .` lets `pytest` work from the backend/ directory without
# requiring PYTHONPATH to be set manually.
pythonpath = .
testpaths = tests
python_files = test_*.py
addopts = -q
