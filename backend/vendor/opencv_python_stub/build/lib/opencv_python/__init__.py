"""Placeholder distribution; see ../pyproject.toml for why this exists.

This module is never imported by name — code always does `import cv2`,
which is provided by the real `opencv-python-headless` dependency instead.
"""
