# Test/dev-only dependencies (installed on top of requirements.txt).
# Run the suite with:  pip install -r requirements-dev.txt && pytest
-r requirements.txt
pytest==9.1.1
httpx==0.27.2   # required by fastapi.testclient.TestClient
